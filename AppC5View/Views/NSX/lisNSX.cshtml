@model IEnumerable<BanGiayTheThao.Models.NSX>

@{
    ViewData["Title"] = "lisNSX";
}

<section class="banner-area organic-breadcrumb">
    <div class="container">
        <div class="breadcrumb-banner d-flex flex-wrap align-items-center justify-content-end">
            <div class="col-first">
                <h1>NSX</h1>
            </div>
        </div>
    </div>
</section>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdNSX)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MaNSX)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TenNSX)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdNSX)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MaNSX)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TenNSX)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>

            <td>
                    
                    @Html.ActionLink("Edit", "Sua", new { id= item.IdNSX }) |
                    @Html.ActionLink("Details", "Details", new { id= item.IdNSX  }) |
                @Html.ActionLink("Delete", "Delete", new { id= item.IdNSX })
            </td>
            
        </tr>
}

    </tbody>
</table>
